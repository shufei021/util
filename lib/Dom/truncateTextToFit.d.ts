/**
 *
 * @param element 要计算宽度的文本元素
 * @param maxWidth 最大宽度
 * @param maxLines 超出的行数就省略
 * @param fontSize 字体大小
 * @returns 合适的文本 + ...
 */
export declare function truncateTextToFit(element: Element, maxWidth: number, maxLines: number, fontSize: number): string;
