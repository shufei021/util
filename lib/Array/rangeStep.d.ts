/**
 * 生成指定范围内指定步长的数组
 * @param {Number} start : 起始值
 * @param {Number} stop : 结束值
 * @param {Number} step : 步长
 */
export declare const rangeStep: (start: number, stop: number, step: number) => number[];
