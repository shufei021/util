/**
 * 对数组进行分组，分组依据是数组中对象的某个属性
 * @param {Array} groups - 要分组的数组
 * @param {String} key - 属性名，用于分组
 * @returns {Array} - 分组后的数组
 */
export declare function groupBy(groups: any[], key: string): Array<any>;
